node_modules
dist
.env
.env.local

# Sauvegarde locale des données de l'ancienne base Django (contient des emails)
backup-donnees-django.json

# Outils locaux de diagnostic
check-site.mjs
check-full.mjs
